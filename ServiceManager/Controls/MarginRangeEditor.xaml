<UserControl x:Class="ServiceManager.Controls.MarginRangeEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="600">
    <StackPanel Margin="0,8,0,0">
        <TextBlock Text="Zakresy marży" Margin="0,0,0,4" FontWeight="SemiBold" />
        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Od" FontWeight="Bold" Margin="2" Grid.Column="0" />
            <TextBlock Text="Do" FontWeight="Bold" Margin="2" Grid.Column="1" />
            <TextBlock Text="Marża (%)" FontWeight="Bold" Margin="2" Grid.Column="2" />
        </Grid>
        <StackPanel x:Name="RowsPanel" />
        <Button x:Name="AddButton" Content="Dodaj zakres" Margin="0,6,0,0" Click="AddButton_Click" />
    </StackPanel>
</UserControl>
