<UserControl x:Class="ServiceManager.Controls.DeliveryEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="600">
    <StackPanel Margin="6">
        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Długość" FontWeight="Bold" Margin="2" Grid.Column="0" />
            <TextBlock Text="Szerokość" FontWeight="Bold" Margin="2" Grid.Column="1" />
            <TextBlock Text="Wysokość" FontWeight="Bold" Margin="2" Grid.Column="2" />
            <TextBlock Text="Waga" FontWeight="Bold" Margin="2" Grid.Column="3" />
            <TextBlock Text="Nazwa" FontWeight="Bold" Margin="2" Grid.Column="4" />
        </Grid>
        <StackPanel x:Name="RowsPanel" />
        <Button x:Name="AddButton" Content="Dodaj dostawę" Margin="0,6,0,0" Click="AddButton_Click" />
    </StackPanel>
</UserControl>
